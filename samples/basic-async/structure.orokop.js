const structure = {
    "example.html": ["fetch-html.js", {
        url: "http://example.com"
    }]
}

const options = {
    output: "output",
    templates: "scripts",
    soft: false
}

module.exports = { structure, options }
